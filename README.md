# python_data_pipeline
A modular Python pipeline that reads raw data from CSV, JSON or APIs, cleans and transforms it, and outputs structured results. It handles missing values, type conversions and edge cases, with built-in logging and config management. Includes sample data and a README with setup steps.
